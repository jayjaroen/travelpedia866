<h1 class="font-bold text-4xl text-pink-600"> Let's go on holiday </h1>

<% @locations.each do |location| %>
  <%= location.name %>
  <%= link_to 'itineraries', location_itineraries_path(location) %>
<% end %>

<hr>
